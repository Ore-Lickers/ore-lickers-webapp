export type ImageResponse = {
  sys: {
    id: string;
  };
  title: string;
  url: string;
  height: number;
  width: number;
};

export type ImageType = {
  id: string;
  title: string;
  url: string;
  height: number;
  width: number;
};
